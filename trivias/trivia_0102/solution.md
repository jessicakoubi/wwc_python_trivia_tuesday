# Solution

**A**

In Python, a string object is an immutable sequence of unicode code points. As such, if you ask for the length of
a string, you will get the number of characters in it or more precisely, the number of code points that compose the string.
